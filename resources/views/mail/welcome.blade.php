<!DOCTYPE html>
<html>
    <head>
        <title>{{ $subject }}</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width-device-width , initial-scale=1 , shrink-to-fit=no">
    </head>

    <body>
            <h4>{{ $subject }}</h4>
            <p>{{ $mailMessage }}</p>
    </body>

</html>
